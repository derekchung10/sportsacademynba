# Django
DJANGO_SECRET_KEY=change-me-in-production
DEBUG=True

# PostgreSQL
DB_NAME=academy_outreach
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# LLM (mock works out of the box — no API key needed)
LLM_PROVIDER=mock
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# NBA policy
MAX_ATTEMPTS_PER_CHANNEL=3
COOLDOWN_HOURS_AFTER_NO_RESPONSE=24
ESCALATION_AFTER_FAILED_ATTEMPTS=5
